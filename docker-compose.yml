version: '3.8'

services:
  grade-evidence-certainty-engine:
    build: .
    container_name: grade-evidence-certainty-engine
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=grade-evidence-certainty-engine-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
